---
ticket: AF-53
title: 'AF Service — expose the Agent Factory command surface over authenticated HTTP (af serve)'
type: feature
status: backlog
priority: high
complexity: high
created: '2026-06-03T12:00:00.000Z'
updated: '2026-06-03T12:00:00.000Z'
design: docs/designs/AF-53-service.md
loka-ref: 8cea7fc6-b80d-4a39-8722-6febdee8b6dd
---

# AF Service — Agent Factory over HTTP (`af serve`)

## Objective
Expose the Agent Factory command surface as an authenticated HTTP service (`af serve`,
systemd on Hanuman) so ORA and remote ORA-clones dispatch and query through one governed
door instead of ad-hoc `bash → af` / `ssh … af`. Central, not federated — one process,
one global concurrency queue, one durable job registry.

## Context
Full technical design: `docs/designs/AF-53-service.md`. Companion to **ORA-160**
(*Deterministic Agent Dispatch*, `oracle-bridge` repo) — the ORA-side dispatch tool that
calls this service. The surface splits into two planes:

- **Execution plane** (`agent spawn`, `orchestrate`, `pipeline run/pause/resume`) — flows
  through a global concurrency queue (`AF_MAX_CONCURRENCY=20`), a SQLite job registry, and
  completion callbacks. Async: dispatch returns a ticket, the result is pushed back.
- **Query / mutation plane** (`projects`, `status`, `task *`, `agent`, `pipeline list/status`,
  `sync`, `init`) — fast synchronous request/response, never queued.

Built in two stages (design §2, §11):
- **Stage A — Execution plane** (governance-critical; ORA-160 Phase 2 depends on it):
  `af serve` skeleton + auth + `POST /jobs` + global queue + SQLite registry +
  reconcile-on-boot + completion callbacks + `/health`. Role: **ENGINEER**.
- **Stage B — Full surface**: presentation-free core-function refactor (§3.1) + query/mutation
  routes. Role: **ENGINEER** (may involve FRONTEND_ENGINEER later if `web/` is pointed at the API).

## Acceptance (from design §13)
- [ ] Auth: unauthenticated / wrong-secret request → 401
- [ ] Project guardrail: `POST /jobs` with missing/unknown `project` → 400, nothing enqueued
- [ ] Concurrency: 25 jobs from two clients → ≤20 run concurrently, rest queue, none dropped
- [ ] Callback on all terminal states (completed/failed/crashed/timeout) — exactly once, no hang
- [ ] Restart survival: queued jobs resume, orphaned `running` rows → failed and re-dispatchable
- [ ] Query plane is unqueued: `GET /projects` returns immediately while 20 jobs run
- [ ] Parity: a representative query/mutation route returns the same data as the equivalent `af` CLI command
- [ ] Loka agreement: `/jobs/:id` outcome matches the Loka task comment for that ticket

## Feature Flag
`ENABLE_AF_53` (default **off**) — gates the whole service (design §8).

## Implementation Role
ENGINEER (Stage A then Stage B).

## Dependencies
- **ORA-160** — the consumer (its dispatch tool calls `POST /jobs`; its callback server receives results)
- AF engine — `dispatchAgent`, orchestrator, `spawn-runner.js` (exist; `ENABLE_AF_48` on)
- Core-function refactor (design §3.1) — prerequisite for Stage B breadth
- Loka sync (AF-12) — unchanged
- Tailscale mesh (exists)

## Log
- [2026-06-03T12:00:00.000Z] architect: spawn.start | Created task file from existing design doc docs/designs/AF-53-service.md. Counter was already at 53 and the design was written, but no task file or Loka ticket existed — this reconciles the ghost ticket into the board.
